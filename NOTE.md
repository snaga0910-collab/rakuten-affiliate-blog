# note 運用手順（消耗品アカウント）

比較記事を note にも投稿するための手順書。
アカウント: **https://note.com/kaisyainkurashi**（投資noteとは別アカウント）

## 前提：投資アカウントと混ざらないようにする

`Note投稿くん` は、ログインしたセッションを `~/.note-state.json` に保存します。
このままだと**投資アカウントのセッションを上書きしてしまう**ため、
消耗品アカウント用は別ファイルに分けます。環境変数 `NOTE_POST_MCP_STATE_PATH` で指定できます。

```bash
export NOTE_POST_MCP_STATE_PATH="$HOME/.note-state-kaisyainkurashi.json"
```

以下の手順は、この環境変数を設定した状態で実行してください。
（毎回打つのが面倒なら、後述のラッパースクリプトを使う）

## 手順

### 1. 記事を note 用に変換する（このリポジトリ）

```bash
npm run note              # 全記事
npm run note oral-care    # 1記事だけ
```

`note-out/<slug>.md` が生成されます。ブログ用の Markdown をそのまま貼ると
**比較表が消えてしまう**（note はテーブル非対応）ため、この変換が必須です。
変換で行っていること:

- 比較表 → 箇条書き（商品名・スペック・生URL）に展開
- `[テキスト](URL)` → テキスト＋生URL（note は生URLを自動リンク化）
- 冒頭にアフィリエイト表示を挿入（ステマ規制対応）
- note用のタグを付与

### 2. 消耗品アカウントでログインする（初回のみ・自分で操作）

```bash
cd "/Users/nagakurashota/Documents/Note投稿くん"
NOTE_POST_MCP_STATE_PATH="$HOME/.note-state-kaisyainkurashi.json" npm run login
```

ブラウザが開くので、**kaisyainkurashi アカウントでログイン**してから
ターミナルに戻って Enter。セッションが保存されます。
（パスワード入力は必ず自分で行ってください）

### 3. 下書きとして投稿する

```bash
cd "/Users/nagakurashota/Documents/Note投稿くん"
NOTE_POST_MCP_STATE_PATH="$HOME/.note-state-kaisyainkurashi.json" \
  node scripts/publish-hybrid.js \
  "/Users/nagakurashota/Documents/rakuten-affiliate-blog/note-out/oral-care.md" \
  "" draft
```

第3引数を `draft` にすると下書き保存で止まります。
**必ず下書きで確認してから、note の画面上で公開してください。**

## 公開前チェックリスト

- [ ] 比較表が箇条書きとして読める形になっているか
- [ ] 楽天のリンクがリンクとして機能しているか（クリックできるか）
- [ ] 冒頭のアフィリエイト表示が消えていないか
- [ ] `〔実体験: 〕` の記号が残っていないか（オーラルケア記事）
- [ ] タグが5個前後付いているか

## 重要：ブログと note で同じ記事を出すときの注意

同じ本文をブログと note の両方に丸ごと載せると、検索エンジンから
**重複コンテンツ**とみなされ、どちらか一方しか評価されないことがあります。
しかも note はドメインが強いため、**note が勝ってブログが育たない**という
本末転倒が起きがちです。

対策として、次のどちらかを推奨します。

**A. note を「入口」にする（おすすめ・当面はこれ）**
ブログを公開するまでは note に全文を載せて、流入と反応を見る。
ブログ公開後は、note 側の記事末に「最新版・比較表はブログで」とリンクを足す。

**B. note は要約版にする**
note には導入＋比較まとめ＋結論だけを載せ、詳細な比較表と各商品レビューは
ブログへ誘導する。ブログを本命の資産として育てたい場合はこちら。

いまはブログが未公開なので **A** で進め、ブログ公開のタイミングで
B へ寄せていくのが無理のない流れです。

## プロフィール設定（note の画面で設定）

一貫性があるほど読者にもアルゴリズムにも伝わりやすいので、次の内容を推奨します。

- **クリエイター名**: ひとり暮らしの消耗品ノート
- **自己紹介**:
  > 一人暮らしの会社員です。洗剤・オーラルケア・コーヒーなど、定期的に買い替える
  > 消耗品を、実際の価格とレビューをもとに正直に比較しています。
  > 良いところだけでなく、合わない場面も書きます。
  > ※記事にはアフィリエイト広告を含みます。
- **ヘッダー画像**: 生活感のあるシンプルな写真（洗面所・キッチンなど）

## 投稿ペース

記事は5本あります。**1日1本ずつ、5日に分けて投稿**するのがおすすめです。
一度に全部出すより、毎日更新されるアカウントの方が note 内で拾われやすくなります。

## 二重投稿の防止（2026-08-13に一度やらかした）

`./scripts/note-post.sh <slug>` は、投稿の直前に note の記事一覧を取得し、
**同じタイトルがすでにあれば中止する**。

一度、すでに公開済みの記事の下書きを重ねて作ってしまった。原因は、
セッション前半に取得した公開リストを、投稿直前に取り直さなかったこと。
その間にユーザーが2本公開しており、古い情報のまま「未公開」と判断していた。

現状だけを見たいときは:

```bash
cd ../Note投稿くん
NOTE_POST_MCP_STATE_PATH="$HOME/.note-state-kaisyainkurashi.json" \
  node scripts/list-notes.mjs
```

**記憶や前回の出力に頼らず、毎回ここで取り直すこと。**

