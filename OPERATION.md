# 運用ガイド（認知拡大とPDCA）

比較記事ブログ「ひとり暮らしの消耗品ノート」の運用手順。
収益ゼロから、検索とPinterestで流入を作るまでの回し方をまとめる。

- ブログ: https://rakuten-affiliate-blog.vercel.app
- note: https://note.com/kaisyainkurashi
- 記事の作り方: [README.md](README.md) / note投稿: [NOTE.md](NOTE.md)

---

## 1. チャネルの役割分担

| チャネル | 役割 | 効き始める時期 |
|---------|------|--------------|
| **Google検索** | 本命の集客。記事数がそのまま的の数 | 1〜3か月 |
| **Pinterest** | 画像検索からの流入。ピンは資産として残る | 2週間〜2か月 |
| **note** | note内の回遊で初速をつくる | 即日 |
| **楽天ROOM** | 既存フォロワー(894人)への告知 | 即日 |

**Xの投資アカウント(@syakaijintoushi)はここには使わない。** 読者層が違ううえ、
投資アカウントのブランドが薄まるため。混ぜないこと。

## 2. 毎週やること（30〜60分）

### 月曜: Plan
- 先週の数字を見て、今週どこに力を入れるか決める（下の「見る数字」参照）

### 平日: Do
- **note**: 1日1本まで（`./scripts/note-post.sh <slug>` で下書き→note画面で公開）
- **Pinterest**: 週1回まとめて予約（下の「4. Pinterestは予約投稿でまとめる」参照）
- **記事**: 週1本を目安に新規作成

### 日曜: Check → Act
- 数字を記録し、伸びた切り口を厚く、伸びない切り口は捨てる

## 3. 見る数字は4つだけ

多く見すぎると続かないので、これだけに絞る。

| 指標 | どこで見る | 意味 |
|------|-----------|------|
| ① 表示回数（検索） | Search Console | Googleに認識されているか |
| ② クリック数（検索） | Search Console | 記事が選ばれているか |
| ③ ピンのクリック数 | Pinterest アナリティクス | どの切り口が刺さるか |
| ④ **楽天のクリック数・成果** | 楽天アフィリエイト レポート | **最終的な収益** |

記録は `metrics.csv` に週1回追記する（`npm run metrics` で雛形を追記できる）。

## 4. Pinterestは予約投稿でまとめる

毎日ピンを手で投稿していると、それ自体が「作業」になって続かなくなる。
Pinterestには予約投稿機能があるので、**週1回の作業でまとめて仕込む**。

### 手順

1. `npm run pins` と `npm run pintext` を実行（画像と投稿文が `pins/` に出る）
2. Pinterest で「作成」→「ピンを作成」を開く
3. `pins/<slug>-<切り口>.png` をアップロードし、`pins/POST.md` のタイトル・説明文・リンク先を貼る
4. 右上の公開ボタンの横にある日時指定から、**公開日時を先の日付にする**
5. 3〜4を繰り返して1〜2週間分を仕込む

予約できる件数と、何日先まで指定できるかは Pinterest 側の仕様で変わる。
画面に出る上限に従うこと。

### 出すペース

- **1日2〜3枚まで**。一度に大量投稿するとスパム判定を受けることがある
- 1記事につき3枚（悩み訴求→価格訴求→比較訴求）を、日をずらして出す
- 同じ日に同じ記事の3枚をまとめて出さない

### 効果測定

予約したぶんが出きったら、Pinterest アナリティクスで**ピン別のアウトバウンドクリック**を見る。
どの切り口（悩み／価格／比較）が効いたかが分かるので、次の記事のピン文言に反映する。
文言を変えるときは `scripts/pin-copy.mjs` を編集して `npm run pintext` を実行する。

## 5. 判断の基準（Act）

数字を見たあと、何をするかの目安。

- **①表示回数が伸びない** → 記事数が足りない。新規記事を優先する
- **①はあるが②クリックが少ない** → タイトルとメタ説明を見直す
- **③ピンのクリックに偏りがある** → 勝った切り口（価格／悩み／比較）で他ジャンルも作る
- **④楽天クリックはあるが成果ゼロ** → 商品選定を見直す（価格帯・レビュー数）
- **④成果が出たジャンル** → 同じジャンルで記事を増やす（横展開）

## 6. 最初の90日の目安

正直なところ、**最初の1〜2か月は数字がほぼ動かない**。
参考にした事例でも「1か月目0円、2か月目8,000円」だった。
ここで焦って手を広げず、記事とピンを積むのが正解。

| 時期 | やること | 期待できること |
|------|---------|--------------|
| 1か月目 | 記事10本、ピン30枚、note5本 | 検索に載り始める |
| 2か月目 | 記事15本、ピン45枚 | Pinterest経由の流入 |
| 3か月目 | 記事20本 | 検索流入と初成果 |

## 7. 記事を増やす手順（いちばん効く作業）

```bash
# 1. 楽天ツールでジャンルの素材をつくる
cd ../rakuten-affiliate-tool
python -m tool article --genre <slug>       # config.yaml に genre を追加してから

# 2. prompt_article.md を Claude に渡して本文を作成 → article.md として保存

# 3. ブログに取り込む
cd ../rakuten-affiliate-blog
npm run import     # 本文を content/ へ
npm run products   # 比較表の商品画像・評価（data/products.json）
npm run charts     # コスト比較グラフ
npm run thumb      # サムネイル
npm run pins       # Pinterestピン3枚
npm run pintext    # 投稿シート更新
npm run note       # note用に変換

# 4. 回遊とピン文言に新しい slug を登録する
#    lib/related.ts の THEMES        … 使う場所（洗面所/洗濯/キッチン/お風呂）
#    scripts/pin-copy.mjs            … ピン3枚の文言
#    scripts/make-pin-text.mjs       … 検索キーワードとボード
#    scripts/import-articles.mjs     … カード用のカテゴリ名
#    scripts/to-note.mjs             … note のハッシュタグ

# 5. 公開
git add -A && git commit -m "記事追加: <genre>" && git push
```

### 次に狙うジャンル候補（消耗品・買い替え頻度が高いもの）

公開済み: オーラルケア／柔軟剤／コーヒー／浄水器／バスマット／洗濯洗剤／食洗機洗剤／替えブラシ／洗濯槽クリーナー

- 食器用洗剤・スポンジ（キッチンのテーマを厚くする）
- トイレットペーパー（1ロールあたりで比較できる。新テーマ「トイレ」）
- シャンプー・ボディソープ詰め替え（洗面所のテーマを厚くする）
- 掃除用シート・フロアワイパー替え
- 除湿剤・防虫剤（季節性があるので時期を選ぶ）

**同じテーマ内で増やすほうが回遊に効く。** バラバラのジャンルを1本ずつ足すより、
1つのテーマを3〜4本にするほうが「この人はこの分野に詳しい」と読者にも検索エンジンにも伝わる。

## 8. やらないこと

- **同じ記事を大量のSNSに撒く** → 重複コンテンツとみなされる恐れ。noteとブログの2つまで
- **Pinterestに1日10枚以上** → スパム判定のリスク。2〜3枚に留める
- **数字を毎日見る** → 動かないので消耗するだけ。週1回でよい
- **成果が出ないジャンルに固執** → 3か月動かなければ別ジャンルへ
